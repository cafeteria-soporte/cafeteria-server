export enum AuditModule {
    AUTH = 'auth',
    USERS = 'users',
    PRODUCTS = 'products',
    INVENTORY = 'inventory',
    SHIFTS = 'shifts',
    ORDERS = 'orders',
    PAYMENTS = 'payments',
    SETTINGS = 'settings',
}
